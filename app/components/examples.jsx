var React = require("react");

var Examples = (props) => {
        return(
            <div>
                <h1>Examples</h1>
                <p>There are currently no examples</p>
            </div>
        )
    };

module.exports = Examples;
